"                        </div>\n\n                        {/* SeÃ§Ã£o de palpite de artilheiro nos jogos do Brasil */}\n                        {(() => {\n                          const isBrazilMatch = match.homeTeamEn === 'Brazil' || match.awayTeamEn === 'Brazil';\n                          if (!isBrazilMatch) return null;\n\n                          return (\n                            <div className=\"brazil-scorer-section\">\n                              <div className=\"brazil-scorer-title\">\n                                <span>âš½ PALPITE DE ARTILHEIRO (PONTOS EXTRA)</span>\n                              </div>\n                              {canEditBet ? (\n                                <div className=\"brazil-scorer-grid\">\n                                  {BRAZIL_PLAYERS.map((player) => {\n                                    const isSelected = displayDrafts[match.id]?.scorer === player.name;\n                                    return (\n                                      <button\n                                        key={player.name}\n                                        type=\"button\"\n                                        className={`brazil-scorer-card ${isSelected ? 'selected' : ''}`}\n                                        onClick={() => {\n                                          setDraftBets((prev) => {\n                                            const currentHome = prev[match.id]?.homeScore ?? displayDrafts[match.id]?.homeScore ?? '';\n                                            const currentAway = prev[match.id]?.awayScore ?? displayDrafts[match.id]?.awayScore ?? '';\n                                            const nextScorer = isSelected ? null : player.name;\n                                            return {\n                                              ...prev,\n                                              [match.id]: {\n                                                homeScore: currentHome,\n                                                awayScore: currentA
<truncated 3879 bytes>
